<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="siddharthbisht.targettracker"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="mainActivity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="addtargetActivity.AddTargetActivity" />

        <receiver
            android:name="Receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name="Receiver.SetDueOnComplete"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name="Receiver.MarkAsComplete"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="GraphActivity.GraphActivity" />
        <activity android:name="aboutActivity.AboutActivity"></activity>
    </application>

</manifest>